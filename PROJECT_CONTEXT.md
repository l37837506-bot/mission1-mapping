# 극한환경 탐사 미션 — 프로젝트 인수인계 (Claude Code용)

이 문서는 프로젝트 전체 맥락과 현재까지 진행 상황을 담고 있습니다.
Claude Code에서 이 파일을 먼저 읽고 작업을 이어가면 됩니다.

---

## 0. 프로젝트 개요

**무엇을 만드는가**: 서울로봇인공지능과학관 2층에서 진행하는 고등학생 대상
STEAM 로봇 교육 프로그램. UGOT 모듈러 로봇을 활용한 "원자력 시설 사고 대응"
시뮬레이션. 3개의 상호 연결된 미션으로 구성.

**핵심 교육 원리**: "임무가 로봇의 형태를 결정한다" — 바퀴형/스파이더형/4족형
각 로봇의 형태에 따라 갈 수 있는 지형과 수행 가능한 임무가 다름.

**대상**: 고1~2, 15명 (3명씩 5팀)
**시간**: 약 110~120분
**운영일**: 2026. 9. 5. (토)
**장소**: 서울로봇인공지능과학관 2층 (원형 건물, 지름 약 47.8m)

**로봇 조작 방식**: 컨트롤러 기반 원격조종(teleoperation) + FPV 태블릿 화면.
코딩/프로그래밍은 학생 활동에서 의도적으로 제외 (실제 현장 방식 반영).

---

## 1. 미션 구조 (3개, 상호 의존)

### 미션 1 — 바퀴형 로봇 정찰 (이번에 앱 개발한 부분)
- 바퀴형 로봇이 2층 복도 전체를 주행하며 환경 데이터 수집
- FPV 카메라로 벽면/바닥 판넬의 수치를 읽음
- 수집 데이터: **방사선량(mSv/h), 온도(°C), 습도(%)**
- 추가로 교량 위치, 캡슐 위치를 시각적으로 발견해서 지도에 표시
- 여기서 수집한 데이터가 미션 2·3의 판단 근거가 됨

### 미션 2 — 스파이더형 로봇 (별도 진행 중)
- 스파이더형 로봇이 교량(22cm 간격)을 건너 모니터링 룸 진입
- 게이지 패널에서 정답 캡슐 ID 번호를 읽음 (디코이 있음)
- **별도 웹앱 존재**: Firebase 동기화, GitHub Pages 배포
  - `https://137837506-bot.github.io/mission2-panel/`
  - operator 페이지: `/operator.html`

### 미션 3 — 4족보행 Terrier Bot
- 낮은 통로에 진입해 정답 번호 캡슐만 회수
- 앞으로 숙이는 자세(pitch)로 중력 포켓 트레이를 작동시켜 회수
- 회수 후 세미나실 앞 팀별 바닥판(시작/종료 지점)으로 복귀하면 완료

---

## 2. 공간 구조 (2층)

원형 건물. 검은색 영역이 복도(주행 가능 구역), 회색이 방.

- **북(N)**: 화장실, 계단, 엘리베이터 코어
- **서(W)**: 사무 공간 + **세미나실(남서, 조종 본부)**
- **남동(SE)**: 강의실
- **중앙**: 에스컬레이터 + OPEN 보이드
- **동(E)**: 계단 코어

**세미나실 = 조종 본부(조종 본부)**: 남서쪽 위치. 5개 팀 바닥판이 이 앞
δ 구역에 배치됨. 미션 1 시작/종료 지점.

### 구역 시스템 (13개 구역)
그리스 문자 그룹 + 번호:
- **α (북측 3개)**: α1, α2(교량 위치), α3
- **β (동측 2개)**: β1, β2 — **통신 불안정 구역** (경고 사인 부착)
- **γ (남측 5개)**: γ1(캡슐 위치), γ2, γ3, γ4, γ5
- **δ (서측 3개)**: δ1, δ2, δ3 (세미나실 앞/옆)

팀 이름은 그냥 **1팀~5팀** (구역과 헷갈리지 않도록 그리스 문자 안 씀).

---

## 3. 미션 1 매핑 앱 — 현재 상태 (이 폴더의 코드)

### 파일 설명

| 파일 | 용도 |
|---|---|
| `index.html` | **실배포용 완성본**. Firebase REST API 사용, apiKey 불필요. GitHub Pages에 이것만 올리면 됨 |
| `mission1_mapping_app.jsx` | Claude 아티팩트용 React 원본 (window.storage 사용). 참고용 |

### index.html 핵심 구조

- **단일 HTML 파일**: React 18 + Babel Standalone (브라우저 내 JSX 변환) CDN 로드
- **백엔드**: Firebase Realtime Database REST API 직접 호출 (SDK 없음)
  - `DATABASE_URL = 'https://mission1-mapping-default-rtdb.firebaseio.com'`
  - `DB_PATH = 'mission1_v1'`
  - 저장: PUT `/{DB_PATH}/{teamId}/{zoneCode}.json`
  - 읽기: GET `/{DB_PATH}.json` (3초 폴링)
- **라우팅**: URL 해시 기반
  - 홈: `/`
  - 팀: `/#/team/1` ~ `/#/team/5`
  - 강사: `/#/admin`

### 화면 구성

1. **홈**: 팀 선택(5개 버튼) + 강사 화면 진입 + 데이터 초기화
2. **팀 뷰**: 원형 지도(SVG) + 13개 구역 핀 → 탭하면 입력 폼
   - 입력: 방사선량, 온도, 습도
   - 체크: 🌉 교량 발견 / 💊 캡슐 발견 (모든 구역에 있음)
   - 진행률 바 (0/13 ~ 13/13)
3. **강사 뷰**:
   - 팀별 진행 상태 바
   - 방사선량 히트맵 모드 (구역별 색상, 전체평균/팀별 필터)
   - 교량·캡슐 위치 모드 (발견 체크한 구역에 배지)

### 지도 렌더링 (중요)
- **순수 SVG 도형**으로 복도를 그림 (`CleanFloorPlan` 컴포넌트)
- 이전에 실제 건축도면 이미지를 base64로 넣었다가, 너무 지저분해서
  **간소화된 검은 복도 실루엣 SVG로 최종 전환함**
- viewBox: 900x900
- 구역 핀 좌표는 `ZONES` 배열의 x, y 값 (이 좌표계 기준)

---

## 4. 남은 작업 / 다음 단계

### 미션 1 앱 관련
- [ ] Firebase Realtime Database 규칙 설정 필요 (아래 참조)
- [ ] GitHub Pages 배포 (`mission1-mapping` 저장소)
- [ ] 실기기 5대 태블릿에서 동시 접속 테스트
- [ ] 구역 핀 위치 미세 조정 가능성 (실제 복도와 대조)

### 물리 소품 (미개발)
- [ ] 구역 판넬 13개 규격·디자인 (방사선량/온도/습도 표기)
- [ ] 통신 불안정 경고 사인 (β 구역용)
- [ ] 드럼통 모형 (방사성 폐기물 느낌, 공간 채우기)
- [ ] 팀별 바닥판 5개 (세미나실 앞 δ 구역, 시작/종료 지점)
- [ ] 교량 구조물 (미션 2, 22cm 간격)
- [ ] 미션 3 캡슐 회수 트레이 메커니즘

### 공간 디자인 확정 사항
- 바닥 테이프 사용 불가 (자국 문제) → EVA 폼 매트, A형 배너 스탠드로 대체
- 흰 벽 + 밝은 바닥 환경
- 매번 설치/철거 반복 (상설 불가)

---

## 5. Firebase 설정 (필수)

프로젝트: `mission1-mapping`
Database URL: `https://mission1-mapping-default-rtdb.firebaseio.com`

### 규칙 (Realtime Database → 규칙 탭)
```json
{
  "rules": {
    "mission1_v1": {
      ".read": true,
      ".write": true
    }
  }
}
```

이걸 설정 안 하면 저장/읽기가 안 됨. 미션 2 게이지 패널과 같은 Firebase
프로젝트를 쓰려면 DB_PATH만 다르게 하면 데이터 분리 가능.

---

## 6. 배포 방법 (GitHub Pages)

1. `github.com/new` → 저장소 이름 `mission1-mapping` → Public
2. `index.html` 업로드 → Commit
3. Settings → Pages → Deploy from branch → `main` / `(root)` → Save
4. 배포 URL: `https://137837506-bot.github.io/mission1-mapping/`

### 태블릿 URL 배정
- 1~5팀: `.../mission1-mapping/#/team/{1~5}`
- 강사: `.../mission1-mapping/#/admin`
- iOS/Android 모두 "홈 화면에 추가"로 앱처럼 사용 가능

---

## 7. 기술 스택 / 리소스

- **로봇**: UGOT 모듈러 로봇 (UBTECH) — 바퀴형/스파이더형/Terrier Bot 4족
- **FPV**: Wi-Fi 태블릿 제어
- **웹 배포**: GitHub Pages (`137837506-bot.github.io`)
- **DB**: Firebase Realtime Database
- **네트워크 이슈**: 2층 기존 AP 4개, UGOT 15대+태블릿 15대 동시 연결
  대역폭 우려 있음 (팀별 핫스팟 분리 or 교육용 AP 추가 검토 중)

---

## 8. 작업 스타일 참고

- 프로토타입 먼저 검증 후 본제작/업체 의뢰
- 실제 산업/원자력 현장 레퍼런스 중시 (Boston Dynamics Spot@후쿠시마,
  KAERI ARMstrong, 한수원 고리 1호기 4족 로봇 등)
- FPV 전용 조작 원칙 (학생이 현장을 직접 안 보고 카메라만 봄)
- 코어 가이드형 문서 선호 (핵심 질문/주의점/지도 포인트)
- 마크다운 문서, SVG 다이어그램/평면도 선호
